// commands/setup.js
module.exports = {
    name: 'setup',
    description: 'Ticket botu ayarlarını yapılandırır.',
    async execute(message, args) {
        // Ticket rolleri ve genel ayarları yapılandırma kodu
        // Örnek: message.reply('Setup işlemi tamamlandı.');
    },
};
